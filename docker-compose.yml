services:
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: "kksama"
      POSTGRES_PASSWORD: "kksama1"
      POSTGRES_DB: "vortexTaskDB"
    networks:
      - mynetwork


  app:
    build: .
    depends_on:
      - postgres
    restart: always
    ports:
      - "8080:8080"
    networks:
      - mynetwork


networks:
  mynetwork:
    driver: bridge
